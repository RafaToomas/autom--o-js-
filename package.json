{
  "name": "tce-pr-automacao",
  "version": "1.0.0",
  "description": "Automação de consultas do TCE-PR",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "express": "^5.1.0",
    "playwright": "^1.55.0"
  }
}